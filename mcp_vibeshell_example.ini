[vibeshell]
; Long random token used by MCP / tools as Bearer token
token = ABC123

; Root directory for this shell.
; "~" means "this OS user's home directory" (e.g. /home/username).
; You can point it elsewhere if you ever want to lock it down more.
base_dir = ~
